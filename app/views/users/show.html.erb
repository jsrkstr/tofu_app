<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
    <section>
      <%#= render 'shared/stats' %>
    </section>
  </aside>
  <div class="span8">
    <%= render 'connect_form' if signed_in? %>
    <% if current_user?(@user) && @user.tofus.any? %>
      <h3>My Tofus (<%= @user.tofus.count %>)</h3>
      <ol class="tofus">
        <%= render @tofus %>
      </ol>
      <%= will_paginate @tofus %>
    <% end %>
  </div>
</div>